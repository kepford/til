# Expand Alias - Ctrl-Alt-e

To expand an alias in Bash simply hold Ctrl-Alt-e after typing the alias and the
alias will be expanded inline.

Example:

* Type `l`
* Hit Ctrl-Alt-e
* Bash will expand to the full command `ls -lhGt`


